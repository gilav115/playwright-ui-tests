name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: lts/*
        cache: npm

    - name: Install dependencies
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v4
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        # Scope cache to chromium only. To add firefox/webkit, extend the install
        # command below and update this key so the cache invalidates correctly.
        key: playwright-chromium-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

    - name: Install Playwright browsers
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install --with-deps chromium

    - name: Install Playwright system dependencies
      if: steps.playwright-cache.outputs.cache-hit == 'true'
      run: npx playwright install-deps chromium

    - name: Run Playwright tests
      run: npx playwright test --project=chromium --project=auth-suite --project=setup:user --project=setup:admin
      env:
        TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD || 'template_placeholder' }}
        ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD || 'template_placeholder' }}

    # Allure reporting — install allure-playwright and uncomment to enable
    # - name: Generate Allure report
    #   if: always()
    #   run: npx allure generate allure-results --clean -o allure-report

    - name: Upload Playwright report
      uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30

    # - name: Upload Allure report
    #   uses: actions/upload-artifact@v4
    #   if: always()
    #   with:
    #     name: allure-report
    #     path: allure-report/
    #     retention-days: 30
